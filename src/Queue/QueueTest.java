package Queue;

import Queue.tools.impl.QueueToolsImpl;

public class QueueTest {

    public static void main(String[] args) {
        //Examples are listed for your convenient using!
        QueueToolsImpl test = new QueueToolsImpl();
        test.outputWelcomeOnConsole();
        // Example one ：Output the queue which you just input!
        // Of course, you can use non-parameter method or method with parameter!
//        test.outputQueue(test.getQueue());
//        test.outputQueue();
        // Example two ：Delete first element,then output the new queue!
        // Of course, you can use non-parameter method or method with parameter!
        // Of course, you can use decoupling way or not!
//        Object[] queue = test.createQueue();
//        Object[] objects = test.deleteElementFromQueue(queue);
//        test.outputQueue(objects);
//        test.outputQueue(test.deleteElementFromQueue(test.createQueue()));
        // Example three : Update last element,then output the new queue!
        // Of course, you can use non-parameter method or method with parameter!
        // Of course, you can use decoupling way or not!
//        Object[] queue = test.createQueue();
//        Object[] objects = test.updateElementFromQueue(queue, 3);
//        test.outputQueue(objects);
//        test.outputQueue(test.updateElementFromQueue(test.getQueue(),3));
        // Example four : Add new element to the rear of the queue,then output the new queue!
        // Of course, you can use non-parameter method or method with parameter!
        // Of course, you can use decoupling way or not!
//        Object[] queue = test.createQueue();
//        Object[] objects = test.addElementToQueueRear(queue);
//        test.outputQueue(objects);
//        test.outputQueue(test.addElementToQueueRear(test.createQueue()));
        //Example five : Take out the first element,then output the new queue!
        // Of course, you can use non-parameter method or method with parameter!
        // Of course, you can use decoupling way or not!
//        Object[] queue = test.createQueue();
//        Object[] objects = test.takeElementFromQueue(queue);
//        test.outputQueue(objects);
//        test.outputQueue(test.takeElementFromQueue(test.createQueue()));

    }
}
