package LoopQueue;

import LoopQueue.tools.impl.LoopQueueToolsImpl;

public class LoopQueueTest {
    public static void main(String[] args) {
        //Examples are listed for your convenient using!
        LoopQueueToolsImpl test = new LoopQueueToolsImpl();
        test.outputWelcomeOnConsole();
        // Example one : Create a new loop Queue,then output it!
        // Of course, you can use non-parameter method or method with parameter!
        // Of course, you can use decoupling way or not!
//        Object[] loopQueue = test.createLoopQueue(6);
//        test.outputLoopQueue(loopQueue);
//        test.outputLoopQueue(test.createLoopQueue());
        // Example two : Output the size of loop queue which you create,excluding null element!
        // Of course, you can use non-parameter method or method with parameter!
        // Of course, you can use decoupling way or not!
//        Object[] loopQueue = test.createLoopQueue();
//        int size = test.getSize(loopQueue);
//        System.out.println(size);
//        System.out.println(test.getSize(test.createLoopQueue()));
        // Example three : Output the boolean type as the result of whether the queue's size is up to max!
        // Of course, you can use non-parameter method or method with parameter!
        // Of course, you can use decoupling way or not!
//        Object[] loopQueue = test.createLoopQueue();
//        boolean max = test.isMax(loopQueue);
//        System.out.println(max);
//        System.out.println(test.isMax(loopQueue));
        // Example four : Output the boolean type as the result of whether the queue is null!
        // Of course, you can use non-parameter method or method with parameter!
        // Of course, you can use decoupling way or not!
//        Object[] loopQueue = test.createLoopQueue();
//        boolean aNull = test.isNull(loopQueue);
//        System.out.println(aNull);
//        System.out.println(test.isNull(loopQueue));
        // Example five : Add element to the rear of the queue you create,then output it !
        // I provide two method,one is static input, another one is dynamic input!
        // Of course, you can use non-parameter method or method with parameter!
        // Of course, you can use decoupling way or not!
//        Object[] loopQueue = test.createLoopQueue();
//        Object[] addedElement={};
//        Object[] objects = test.addElementToRear(loopQueue, addedElement);
//        test.outputLoopQueue(objects);
//        Object[] objects = test.addElementToRear(loopQueue);
//        test.outputLoopQueue(objects);
//        test.outputLoopQueue(test.addElementToRear(test.createLoopQueue(),test.createLoopQueue()));
        // Example Six : Take the first element from the queue you create!
        // Output what is taken and what remains!
        // Of course, you can use non-parameter method or method with parameter!
        // Of course, you can use decoupling way or not!
//        Object[] loopQueue = test.createLoopQueue();
//        Object[] objects = test.takeFrontElementFromLoopQueue(loopQueue);
//        test.outputLoopQueue(objects);
//        test.outputLoopQueue(test.takeFrontElementFromLoopQueue(test.createLoopQueue()));


    }
}
